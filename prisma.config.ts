// Legacy Prisma 7 config file. With Prisma 5, this is no longer used
// because the datasource URL is defined directly in `prisma/schema.prisma`.
// We keep this file as an empty module so TypeScript compiles cleanly.

export {};
